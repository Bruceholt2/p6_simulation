"Create src/xer_parser.py. Build a class called XERParser that reads a P6 XER file and extracts tables into pandas DataFrames. Requirements:

Accept a file path in the constructor
Try UTF-8 encoding first, fall back to Latin-1
Parse %T, %F, and %R lines to build a dictionary of table name -> DataFrame
Provide a method get_table(table_name) that returns the DataFrame for a given table
Provide convenience properties: tasks, predecessors, calendars, resources, resource_assignments, project
Convert date string columns to pandas datetime objects
Convert numeric fields (durations, floats, quantities) to appropriate numeric types
Add a summary() method that prints how many projects, activities, relationships, calendars, and resources were parsed

Then write pytest tests in tests/test_xer_parser.py. Create a small fixture XER string with 5 activities, 4 FS relationships forming a simple network, 1 calendar, and 1 project. Test that all tables parse correctly and that data types are right."